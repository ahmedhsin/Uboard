/*
import chai from 'chai'
import chaiHttp from 'chai-http'
import app from '../app'
const should = chai.should()
chai.use(chaiHttp)

describe("test a get users", (done: Mocha.Done) => {
    it("test", () => {
        chai.request(app)
        .get("/users")
        .end((err, res) => {
            res.should.have.status(200)
            done()
        })
    })
})
*/